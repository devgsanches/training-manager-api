# Template de Prompt para Criar uma Rota de API

Crie a rota `GET /stats?from=&to=`

## Descrição

Essa rota retorna estatísticas do usuário autenticado em relação a treinos completos e iniciados dentro de um período.

## Requisitos Técnicos

- Use case deve se chamar "GetStats".
- Formato da data recebida deve ser YYYY-MM-DD (usar `z.string().date()` para validação).
- Use a biblioteca dayjs para manipular e formatar datas.
- Criar um novo módulo `stats` em `src/stats/`.

## Autenticação

- Rota protegida.

## Request

```ts
interface Body {}
```

```ts
interface Params {}
```

```ts
interface Query {
  from: string // YYYY-MM-DD
  to: string // YYYY-MM-DD
}
```

## Response

```ts
interface StatusCode200 {
  workoutStreak: number
  consistencyByDay: {
    // key: "2025-01-01" — incluir apenas dias que possuem sessão
    [key: string]: {
      workoutDayCompleted: boolean
      workoutDayStarted: boolean
    }
  }
  completedWorkoutsCount: number
  conclusionRate: number
  totalTimeInSeconds: number
}
```

## Regras de Negócio

- Buscar todas as WorkoutSession do usuário onde startedAt está dentro do range `from` até `to`.
- Agrupar as sessões por data (parte da data de startedAt).
- consistencyByDay deve conter apenas os dias que possuem pelo menos uma sessão (dias sem atividade não entram no map).
- Se um dia de treino foi completado (completedAt preenchido), ele deve ser contabilizado tanto em workoutDayCompleted quanto em workoutDayStarted.
- Dias de descanso (isRest = true) também devem ser contabilizados caso sejam iniciados e/ou concluídos.
- Se um dia de treino foi apenas iniciado (sem completedAt), ele deve ser contabilizado apenas em workoutDayStarted.
- conclusionRate: `completedWorkoutsCount / totalDeSessões` (0 se não houver sessões).
- totalTimeInSeconds: soma de `(completedAt - startedAt)` em segundos para todas as sessões completadas.
- workoutStreak: número de dias em sequência que o usuário completou algum dia de treino. Exemplo: Se o plano de treino tem os dias de treino segunda, terça, quarta, quinta e sexta-feira, e o usuário completou todos (mesmo que algum deles seja dia de descanso) ele terá um streak de 5.

## Referência

Para referência de implementação, veja a rota `GET /home/:date` que tem lógicas parecidas.

## Padrão de Implementação

### DTO (`dto/stats.dto.ts`)

- Zod schema para validação das query params (`from`, `to`).
- Classes com `@ApiProperty` para documentação Swagger, padrão Input/Output por rota (ex: `GetStatsOutputDto`).
- Tudo do mesmo model fica em um único arquivo.

### Repository

- Usar repositórios existentes (`WorkoutPlanRepository`, `WorkoutSessionRepository`) injetados via constructor no use case.

### Use Case (`use-cases/get-stats.use-case.ts`)

- Injectable, recebe repositórios via constructor.
- Lança exceções do NestJS (`NotFoundException`).
- Sem try-catch.

### Controller (`stats.controller.ts`)

- `@Get()` com `@Query()` para receber `from` e `to`.
- Decorators de response do Swagger.
- O controller apenas delega para o use case.

### Module (`stats.module.ts`)

- Registrar controller, providers (use case).
- Importar `WorkoutPlanModule` e `WorkoutSessionModule` para acesso aos repositórios exportados.
- Registrar no `AppModule`.
